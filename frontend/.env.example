# In development the Vite proxy handles /api → :8080, so this is normally not needed.
# In production, if the frontend is served from a different origin than the backend:
# VITE_API_BASE=https://cloudstream-web.example.lan/api/v1
